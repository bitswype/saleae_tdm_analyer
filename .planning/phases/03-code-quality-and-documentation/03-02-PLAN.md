---
phase: 03-code-quality-and-documentation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements: [DOCS-01, DOCS-02]

must_haves:
  truths:
    - "The README build section explains what each step does, not just the commands"
    - "A developer unfamiliar with CMake FetchContent can follow the build instructions and understand what is happening"
    - "The README explains that cmake configure downloads the AnalyzerSDK automatically via FetchContent"
    - "The README explains where the compiled output lands (build directory Analyzers/ subdirectory)"
    - "The WAV export section states the TXT/CSV workaround is the permanent architecture, not a bug"
    - "No hedging language like 'bug' or 'when this analyzer was authored' remains in the WAV export section"
  artifacts:
    - path: "README.md"
      provides: "Updated build instructions and WAV export documentation"
      contains: "FetchContent"
  key_links:
    - from: "README.md"
      to: "cmake/ExternalAnalyzerSDK.cmake"
      via: "documentation reference to FetchContent SDK download"
      pattern: "FetchContent"
---

<objective>
Update README documentation: rewrite build instructions to explain what each step does (not just list commands), and update the WAV export section to state that the TXT/CSV workaround is the permanent Saleae architecture.

Purpose: A developer unfamiliar with CMake FetchContent can follow the build instructions with understanding, and the WAV export documentation no longer implies a temporary limitation that might be fixed.

Output: Updated README.md with explanatory build prose and corrected WAV export framing.
</objective>

<execution_context>
@/home/chris/.claude/get-shit-done/workflows/execute-plan.md
@/home/chris/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-code-quality-and-documentation/03-RESEARCH.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite README build instructions with explanatory prose (DOCS-01)</name>
  <files>README.md</files>
  <action>
Rewrite the "Building instructions" sections (### MacOS, ### Ubuntu 16.04, ### Windows) to explain what each build step does. **Scope: Only modify the build command blocks and add explanatory prose around them. Do NOT touch feature descriptions, error/warning documentation, flag values, install instructions, or debugging instructions.**

**Changes to make:**

1. **Add an introductory paragraph** at the top of the "Building instructions" section (after `# Building instructions`) explaining the overall build process:
   - This project uses CMake with FetchContent to automatically download the Saleae AnalyzerSDK from GitHub during the configure step
   - No manual SDK installation is needed — the first `cmake` configure command handles it (requires internet connection)
   - The build produces a shared library (`libtdm_analyzer.so` on Linux, `libtdm_analyzer.dylib` on macOS, `tdm_analyzer.dll` on Windows) in the `Analyzers/` subdirectory of the build directory

2. **For each platform's build commands**, add brief inline comments or surrounding prose explaining:
   - `cmake -S . -B build` or `cmake ..` — Configures the project: creates build directory, downloads AnalyzerSDK via FetchContent, generates build files
   - `-DCMAKE_BUILD_TYPE=Release` / `Debug` — Selects optimized release build vs debug build with symbols
   - `cmake --build .` or `cmake --build build-release` — Compiles the analyzer shared library
   - `cmake --build . --target clean` — Removes compiled artifacts without removing the downloaded SDK

3. **Do NOT change:**
   - The dependency lists (XCode, CMake 3.13+, gcc 4.8+, Visual Studio 2015, etc.)
   - The MacOS CMake installation instructions
   - The Ubuntu debugging/AppImage instructions
   - The Windows Visual Studio setup notes
   - The "How did I figure some of this stuff out?" section

**Style:** Keep explanations concise — 1-2 sentences per command block. Use code comments (`#`) within code blocks where appropriate. Match the existing README tone (informal, practical).
  </action>
  <verify>
    <automated>cd /home/chris/gitrepos/saleae_tdm_analyer && grep -c 'FetchContent' README.md | grep -qv '^0$' && grep -c 'Analyzers/' README.md | grep -qv '^0$' && echo "PASS" || echo "FAIL"</automated>
    <manual>Read the build sections and confirm each command has an explanation of what it does</manual>
  </verify>
  <done>The build instructions section opens with an explanation of FetchContent-based SDK download. Each platform's build commands include prose or comments explaining what the command does. The output location (Analyzers/ subdirectory) is documented. Dependency lists, debugging instructions, and non-build sections are unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Update WAV export documentation to reflect permanent architecture (DOCS-02)</name>
  <files>README.md</files>
  <action>
Rewrite the opening paragraph of the "Exporting data as a wave file" section (line 95 area) to remove the "bug" framing and state the permanent architecture.

**Current text to replace (approximately lines 95-96):**
```
There is a bug in Logic 2 where the displayed export options are limited to `TXT/CSV`.  This bug is still present in Logic v2.3.58 (when this analyzer was authored).  The way to work around this is to add a setting into the analyzer to select what "export to TXT/CSV" will _actually_ do.
```

**Replace with text conveying:**
- Logic 2 does not support custom export types for Low Level Analyzers
- The TXT/CSV export path is the only export mechanism available to analyzer plugins
- This is a confirmed Saleae design decision, not a temporary limitation or bug
- This analyzer works around this by adding an export format selector in the analyzer settings — the "Export to TXT/CSV" action produces either CSV or WAV output based on that setting

**Remove:** All hedging phrases like "bug", "still present in Logic v2.3.58", "when this analyzer was authored", and any language implying this might be fixed in a future Logic 2 version.

**Keep unchanged:** The numbered steps (1-4) explaining how to export, the "Things to be aware of when exporting a wav file" section, and all technical details about WAV format behavior.

**Tone:** Factual and neutral. Not apologetic, not critical of Saleae. Just state what is and how the analyzer handles it.
  </action>
  <verify>
    <automated>cd /home/chris/gitrepos/saleae_tdm_analyer && grep -ci 'bug' README.md | xargs test 0 -eq && grep -c 'when this analyzer was authored' README.md | xargs test 0 -eq && grep -c 'permanent\|design decision\|does not support custom export' README.md | grep -qv '^0$' && echo "PASS" || echo "FAIL"</automated>
    <manual>Read the WAV export section and confirm no hedging language remains and the permanent architecture is clearly stated</manual>
  </verify>
  <done>The WAV export section states that Logic 2 does not support custom export types as a design decision (not a bug). No references to "bug", "v2.3.58", or "when this analyzer was authored" remain. The export steps and technical details are unchanged.</done>
</task>

</tasks>

<verification>
1. `grep -c 'FetchContent' README.md` returns at least 1 (DOCS-01 — FetchContent explained)
2. `grep -c 'Analyzers/' README.md` returns at least 1 (DOCS-01 — output location documented)
3. `grep -ci 'bug' README.md` returns 0 in the WAV export section context (DOCS-02 — no "bug" framing)
4. `grep -c 'when this analyzer was authored' README.md` returns 0 (DOCS-02 — hedging removed)
5. `grep -c 'design decision\|does not support custom export\|permanent' README.md` returns at least 1 (DOCS-02 — permanent architecture stated)
6. The numbered export steps (1-4) and "Things to be aware of" section are unchanged
</verification>

<success_criteria>
- DOCS-01: Build instructions explain what each step does; FetchContent SDK download is mentioned; output location is documented; a developer unfamiliar with CMake can follow the instructions
- DOCS-02: WAV export section states TXT/CSV workaround is the permanent architecture; no "bug" or "when this analyzer was authored" language remains; export steps and technical details are preserved
</success_criteria>

<output>
After completion, create `.planning/phases/03-code-quality-and-documentation/03-02-SUMMARY.md`
</output>
