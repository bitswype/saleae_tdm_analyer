---
phase: 04-sdk-audit-and-housekeeping
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/TdmAnalyzer.h
  - src/TdmAnalyzer.cpp
  - CHANGELOG.md
  - README.md
autonomous: true
requirements:
  - SDKM-01
  - SDKM-02
  - SDKM-03

must_haves:
  truths:
    - "TdmAnalyzer.h no longer contains a mResultsFrameV2 member variable"
    - "FrameV2 output uses the key \"frame_number\" (not \"frame #\")"
    - "A commit message documents that 114a3b8 is confirmed as AnalyzerSDK HEAD with the verification date"
    - "CHANGELOG.md exists with a v2.0.0 Breaking Changes section containing a before/after Python migration example"
    - "README.md contains a migration note about the frame_number key rename"
    - "An annotated v2.0.0 git tag exists on the final commit of this phase"
  artifacts:
    - path: "src/TdmAnalyzer.h"
      provides: "Clean class with mResultsFrameV2 removed"
      contains: "bool mSimulationInitilized"
    - path: "src/TdmAnalyzer.cpp"
      provides: "FrameV2 key rename"
      contains: "frame_number"
    - path: "CHANGELOG.md"
      provides: "Breaking change documentation with migration example"
      contains: "frame_number"
    - path: "README.md"
      provides: "Migration note for HLA script authors"
      contains: "frame_number"
  key_links:
    - from: "CHANGELOG.md"
      to: "src/TdmAnalyzer.cpp"
      via: "Documents the key rename from frame # to frame_number"
      pattern: "frame_number"
    - from: "README.md"
      to: "CHANGELOG.md"
      via: "References CHANGELOG.md for full change list"
      pattern: "CHANGELOG.md"
---

<objective>
Verify the SDK pin, remove dead code, rename the FrameV2 key, and document the breaking change for v2.0.0.

Purpose: Phase 4 is housekeeping — the SDK pin is confirmed current, a dead member variable is removed to reduce confusion, and the FrameV2 key is renamed so HLA Python scripts can access it without workarounds. The key rename is a breaking change that requires a major version bump, CHANGELOG.md, and a README migration note.

Output: Three semantic commits (SDK audit, source changes, documentation), a new CHANGELOG.md, an updated README.md, and an annotated v2.0.0 git tag.
</objective>

<execution_context>
@/home/chris/.claude/get-shit-done/workflows/execute-plan.md
@/home/chris/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-sdk-audit-and-housekeeping/04-RESEARCH.md
@.planning/phases/04-sdk-audit-and-housekeeping/04-CONTEXT.md
@src/TdmAnalyzer.h
@src/TdmAnalyzer.cpp
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: SDK audit, dead member removal, and FrameV2 key rename</name>
  <files>
    src/TdmAnalyzer.h
    src/TdmAnalyzer.cpp
  </files>
  <action>
This task produces three separate semantic git commits on main.

**Commit 1 — SDK audit (SDKM-01):**
Run `git ls-remote https://github.com/saleae/AnalyzerSDK.git HEAD` to verify the current HEAD SHA.
Create a commit with NO file changes (documentation-only commit message). The commit message is the deliverable:

```
chore(04): SDK audit — 114a3b8 confirmed current HEAD (YYYY-MM-DD)

AnalyzerSDK HEAD verified via:
  git ls-remote https://github.com/saleae/AnalyzerSDK.git HEAD

Result: 114a3b8306e6a5008453546eda003db15b002027

This is the same commit pinned in cmake/ExternalAnalyzerSDK.cmake.
No SDK update is needed.

Closes SDKM-01.
```

Use `git commit --allow-empty` since there are no file changes — the commit message IS the audit record. Use today's actual date from the ls-remote verification.

**Commit 2 — Dead member removal (SDKM-02):**
In `src/TdmAnalyzer.h`, remove line 40 (`FrameV2 mResultsFrameV2;`). The result should be:

```cpp
    Frame mResultsFrame;
    bool mSimulationInitilized;
```

Do NOT touch `UseFrameV2()` in the constructor (TdmAnalyzer.cpp line 13) — that call is required and unrelated to the member variable.

After removing the line, optionally add a comment above `UseFrameV2()` in TdmAnalyzer.cpp constructor:
```cpp
    // Required: registers this analyzer as a FrameV2 producer.
    // Without this, AddFrameV2() data is silently dropped and the data table will be empty.
    // Do not remove. Requires Logic 2.3.43+.
    UseFrameV2();
```

Stage src/TdmAnalyzer.h (and TdmAnalyzer.cpp if the comment was added) and commit:
```
fix(04): remove unused mResultsFrameV2 member variable

The FrameV2 mResultsFrameV2 member in TdmAnalyzer.h was never read,
written, or passed anywhere. AnalyzeTdmSlot() correctly uses a local
FrameV2 variable on the stack. Removing the dead member eliminates
confusion about which FrameV2 object is active.

UseFrameV2() in the constructor is NOT affected — it is a required
SDK registration call, not related to the member variable.

Closes SDKM-02.
```

**Commit 3 — FrameV2 key rename (SDKM-03):**
In `src/TdmAnalyzer.cpp`, change line 336 from:
```cpp
    frame_v2.AddInteger("frame #", mFrameNum);
```
to:
```cpp
    frame_v2.AddInteger("frame_number", mFrameNum);
```

This is the breaking change. Use `"frame_number"` exactly (not `"frame_num"`) per user decision.

Stage src/TdmAnalyzer.cpp and commit:
```
feat!(04): rename FrameV2 key "frame #" to "frame_number"

BREAKING CHANGE: The FrameV2 field key "frame #" has been renamed to
"frame_number". HLA Python scripts that access frame.data["frame #"]
must be updated to frame.data["frame_number"].

The space and hash character in the old key broke Python attribute-style
access and could cause issues with future Saleae tooling. The new name
follows Saleae's CAN analyzer naming conventions (full words,
underscore-separated).

Closes SDKM-03.
```
  </action>
  <verify>
    <automated>cd /home/chris/gitrepos/saleae_tdm_analyer && grep -c "mResultsFrameV2" src/TdmAnalyzer.h | grep -q "^0$" && grep -q '"frame_number"' src/TdmAnalyzer.cpp && ! grep -q '"frame #"' src/TdmAnalyzer.cpp && echo "PASS"</automated>
    <manual>Verify git log shows three separate commits with the expected messages</manual>
  </verify>
  <done>
    - mResultsFrameV2 is absent from TdmAnalyzer.h
    - "frame_number" appears in TdmAnalyzer.cpp where "frame #" used to be
    - UseFrameV2() is still present in the constructor
    - Three semantic commits exist: SDK audit (empty), dead member removal, key rename
  </done>
</task>

<task type="auto">
  <name>Task 2: Breaking change documentation and v2.0.0 tag</name>
  <files>
    CHANGELOG.md
    README.md
  </files>
  <action>
This task creates documentation for the breaking change and tags the release.

**Step 1 — Create CHANGELOG.md:**
Create a new `CHANGELOG.md` at the repo root using Keep a Changelog format. Content:

```markdown
# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.0.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]

## [2.0.0] - YYYY-MM-DD

### Breaking Changes

- **FrameV2 key rename**: The `"frame #"` field key has been renamed to `"frame_number"`.
  HLA scripts that access this field must be updated.

  **Before:**
  ```python
  frame_number = frame.data["frame #"]
  ```

  **After:**
  ```python
  frame_number = frame.data["frame_number"]
  ```

### Changed

- Renamed FrameV2 field key `"frame #"` to `"frame_number"` for HLA compatibility.
  Keys with spaces or special characters can break Python attribute-style access
  and may cause issues with future Saleae tooling.

### Removed

- Removed unused `mResultsFrameV2` member variable from `TdmAnalyzer` class.
  This was dead code — the implementation correctly uses a local `FrameV2` variable
  in `AnalyzeTdmSlot()`.

[Unreleased]: https://github.com/bitswype/saleae_tdm_analyer/compare/v2.0.0...HEAD
[2.0.0]: https://github.com/bitswype/saleae_tdm_analyer/releases/tag/v2.0.0
```

Use today's actual date for the `[2.0.0]` entry. Note the GitHub URL uses the actual remote: `https://github.com/bitswype/saleae_tdm_analyer`.

**Step 2 — Add migration note to README.md:**
Add a "Migration Guide" section to README.md. Place it after the "# TDM Analyzer" heading and description but before "# Features" — this makes the breaking change prominent for anyone visiting the repo. The section should be:

```markdown
# Migration Guide

## v2.0.0 — FrameV2 key rename

The `"frame #"` FrameV2 field has been renamed to `"frame_number"`.
Update any HLA scripts that access this field:

```python
# Before (v1.x)
frame_number = frame.data["frame #"]

# After (v2.0+)
frame_number = frame.data["frame_number"]
```

See [CHANGELOG.md](CHANGELOG.md) for the full list of changes.
```

**Step 3 — Commit documentation:**
Stage CHANGELOG.md and README.md and commit:

```
docs(04): add CHANGELOG.md and README migration note for v2.0.0

Create CHANGELOG.md following Keep a Changelog format with:
- Breaking Changes section documenting "frame #" → "frame_number" rename
- Before/after Python migration example
- Changed and Removed sections

Add Migration Guide section to README.md referencing the breaking change
and linking to CHANGELOG.md.

Part of v2.0.0 release (SDKM-01, SDKM-02, SDKM-03).
```

**Step 4 — Create annotated v2.0.0 tag:**
Create an annotated tag (NOT lightweight) on the documentation commit:

```bash
git tag -a v2.0.0 -m "v2.0.0 — SDK audit, dead code removal, FrameV2 key rename (breaking)"
```

Do NOT push the tag — the user will push when ready.
  </action>
  <verify>
    <automated>cd /home/chris/gitrepos/saleae_tdm_analyer && test -f CHANGELOG.md && grep -q "frame_number" CHANGELOG.md && grep -q 'frame.data\["frame #"\]' CHANGELOG.md && grep -q 'frame.data\["frame_number"\]' CHANGELOG.md && grep -q "Migration Guide" README.md && grep -q "CHANGELOG.md" README.md && git tag -l v2.0.0 | grep -q "v2.0.0" && echo "PASS"</automated>
    <manual>Review CHANGELOG.md formatting against Keep a Changelog conventions; verify README migration note is visible and well-placed</manual>
  </verify>
  <done>
    - CHANGELOG.md exists at repo root with Keep a Changelog format
    - CHANGELOG.md has a v2.0.0 Breaking Changes section with before/after Python example
    - README.md has a Migration Guide section with the key rename documented
    - README.md references CHANGELOG.md
    - An annotated v2.0.0 git tag exists pointing to the final commit
  </done>
</task>

</tasks>

<verification>
After both tasks complete, verify the full phase:

1. `grep -c "mResultsFrameV2" src/TdmAnalyzer.h` returns 0
2. `grep "frame_number" src/TdmAnalyzer.cpp` shows the renamed key
3. `grep -c "frame #" src/TdmAnalyzer.cpp` returns 0 (old key gone)
4. `grep "UseFrameV2" src/TdmAnalyzer.cpp` confirms the constructor call is still present
5. `test -f CHANGELOG.md` confirms the file exists
6. `grep "Breaking Changes" CHANGELOG.md` confirms the section exists
7. `grep "Migration Guide" README.md` confirms the README section exists
8. `git log --oneline -4` shows the four commits (SDK audit, dead member, key rename, docs)
9. `git tag -v v2.0.0` shows the annotated tag with its message
10. `git describe --tags` returns `v2.0.0` (confirming annotated tag works with git describe)
</verification>

<success_criteria>
- All three SDKM requirements (SDKM-01, SDKM-02, SDKM-03) are closed
- Four semantic commits on main: SDK audit, dead member removal, key rename, documentation
- CHANGELOG.md follows Keep a Changelog with v2.0.0 entry including Python code diff
- README.md has a Migration Guide section before Features
- Annotated v2.0.0 tag exists on the final commit
- Project compiles (no build errors from member removal)
</success_criteria>

<output>
After completion, create `.planning/phases/04-sdk-audit-and-housekeeping/04-01-SUMMARY.md`
</output>
