---
phase: 09-core-wav-writing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - hla/TdmWavExport.py
autonomous: true
requirements: [REQ-08, REQ-09, REQ-10, REQ-11, REQ-12, REQ-13, REQ-14, REQ-15]

must_haves:
  truths:
    - "decode() writes one interleaved PCM frame to the WAV file for each complete TDM frame"
    - "Only slots listed in the slots setting produce WAV channels — all others are discarded"
    - "Channels appear in the WAV in the order slots were specified (not slot-number order)"
    - "A slot absent from a TDM frame is written as silence (zero) rather than causing a crash or drift"
    - "WAV file is not created until the first slot frame arrives (lazy open)"
    - "Sample rate is derived automatically from frame timing — no extra setting required"
    - "Partial captures are always playable because wave.writeframes() patches the header after every write"
  artifacts:
    - path: "hla/TdmWavExport.py"
      provides: "Full decode() implementation: slot filter, accumulator, WAV lifecycle, header refresh"
      exports: ["parse_slot_spec", "_as_signed", "TdmWavExport"]
      min_lines: 100
  key_links:
    - from: "TdmWavExport.decode()"
      to: "self._wav.writeframes()"
      via: "_write_wav_frame() called when frame_number changes"
      pattern: "writeframes"
    - from: "TdmWavExport.__init__()"
      to: "parse_slot_spec()"
      via: "self._slot_list = parse_slot_spec(self._slots_raw)"
      pattern: "parse_slot_spec"
    - from: "_write_wav_frame()"
      to: "self._accum.get(slot, 0)"
      via: "fills silence for missing slots per REQ-11"
      pattern: "_accum.get"
---

<objective>
Implement the full decode() method and WAV writing machinery in hla/TdmWavExport.py, transforming the Phase 8 no-op scaffold into a working Logic 2 HLA that writes multi-channel PCM WAV files from selected TDM slots.

Purpose: This is the core deliverable of v1.5 — without it, the HLA produces no output. Phase 8 built the skeleton; Phase 9 makes it functional.
Output: A single modified hla/TdmWavExport.py containing parse_slot_spec(), _as_signed(), and four private methods (_open_wav, _try_derive_sample_rate, _write_wav_frame, _try_flush) plus a complete decode() body.
</objective>

<execution_context>
@/home/chris/.claude/get-shit-done/workflows/execute-plan.md
@/home/chris/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-core-wav-writing/09-RESEARCH.md
@.planning/phases/08-hla-scaffold-settings/08-01-SUMMARY.md

<interfaces>
<!-- Existing scaffold in hla/TdmWavExport.py — executor modifies this file in-place. -->
<!-- Phase 8 established these patterns — Phase 9 fills in the blanks. -->

From hla/TdmWavExport.py (current scaffold):
```python
from saleae.analyzers import HighLevelAnalyzer, AnalyzerFrame, StringSetting, ChoicesSetting

class TdmWavExport(HighLevelAnalyzer):
    # Class-level settings (injected by Logic 2 before __init__ runs)
    slots = StringSetting(label='Slots (e.g. 0,2,4 or 0-3)')
    output_path = StringSetting(label='Output Path (absolute path to .wav file)')
    bit_depth = ChoicesSetting(['16', '32'], label='Bit Depth')
    bit_depth.default = '16'

    result_types = {
        'status': {'format': '{{data.message}}'},
        'error':  {'format': 'Error: {{data.message}}'}
    }

    def __init__(self):
        self._slots_raw = self.slots
        self._output_path = self.output_path
        self._bit_depth = int(self.bit_depth or '16')
        # Phase 9 adds: slot parsing, WAV file open, state initialization

    def decode(self, frame: AnalyzerFrame):
        if frame.type != 'slot':
            return None
        return None  # Phase 9 replaces this
```

FrameV2 fields available in frame.data (from TDM LLA ten-field schema):
- frame.data['slot']         — int, slot index
- frame.data['data']         — int, sample value (sign-adjusted if LLA Signed=true)
- frame.data['frame_number'] — int, TDM frame counter (increments each complete TDM frame)
- frame.data['short_slot']   — bool, error flag
- frame.data['bitclock_error'] — bool, error flag
- frame.type                 — str, 'slot' or 'advisory'
- frame.start_time           — saleae.data.GraphTime object (subtraction yields GraphTimeDelta; float() gives seconds)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Module-level helpers — parse_slot_spec and _as_signed</name>
  <files>hla/TdmWavExport.py</files>
  <behavior>
    - parse_slot_spec("0,2,4") returns [0, 2, 4]
    - parse_slot_spec("0-3") returns [0, 1, 2, 3]
    - parse_slot_spec("1,3-5,7") returns [1, 3, 4, 5, 7]
    - parse_slot_spec("2,2,2") returns [2] (deduplication, order preserved)
    - parse_slot_spec("4,2,0") returns [4, 2, 0] (insertion order preserved — NOT sorted ascending)
    - parse_slot_spec("5-2") raises ValueError (invalid range)
    - parse_slot_spec("") raises ValueError (no slots)
    - parse_slot_spec("  0 , 2 ") returns [0, 2] (whitespace tolerance)
    - _as_signed(0x8000, 16) returns -32768 (MSB set = negative)
    - _as_signed(0x7FFF, 16) returns 32767 (max positive, no-op)
    - _as_signed(0, 16) returns 0
    - _as_signed(0x80000000, 32) returns -2147483648
    - _as_signed(65535, 16) returns -1 (unsigned max = signed -1)
  </behavior>
  <action>
    Add two module-level functions above the TdmWavExport class definition. Do NOT put them inside the class — they are standalone helpers.

    Function 1: parse_slot_spec(spec: str) -> list
    - Split on ',' to get tokens
    - Strip whitespace from each token
    - Skip empty tokens
    - If token contains '-', split on '-' (maxsplit=1), extend range(int(a), int(b)+1)
    - If a > b in a range token, raise ValueError(f"Invalid range: {token!r}")
    - If token is a plain integer, append int(token)
    - After collecting all slots, if the list is empty raise ValueError(f"No slots parsed from: {spec!r}")
    - Return list(dict.fromkeys(slots)) — preserves insertion order while deduplicating (REQ-09: channels appear in the order the user specified)
    - Do NOT use sorted(set(slots)) — that would sort ascending regardless of user-specified order

    Function 2: _as_signed(value: int, bit_depth: int) -> int
    - mask = (1 << bit_depth) - 1
    - value = value & mask
    - if value >= (1 << (bit_depth - 1)): value -= (1 << bit_depth)
    - return value
    - This handles both already-signed (no-op for in-range values) and unsigned-style large ints

    Also add these imports at the top of the file (after the existing saleae import):
    - import wave
    - import struct

    Then update __init__ to initialize all Phase 9 state:
    - self._slot_list = parse_slot_spec(self._slots_raw)  — ordered slot list (REQ-09)
    - self._slot_set = set(self._slot_list)               — O(1) membership test (REQ-10)
    - self._wav = None                                     — lazy-opened on first frame (REQ-12)
    - self._sample_rate = None                             — derived from timing
    - self._accum = {}                                     — dict[slot_int -> sample_int] for current TDM frame
    - self._last_frame_num = None                         — track frame_number boundaries
    - self._timing_ref = {}                               — dict[slot -> first start_time seen]
    - self._frame_count = 0                               — count of complete TDM frames written

    Remove the "# Phase 9 adds: ..." comment from __init__ — it is no longer accurate.

    Write a test block at the bottom of the file guarded by `if __name__ == '__main__':` that exercises parse_slot_spec and _as_signed with assert statements covering the behavior cases above. This is the automated verification target — it must pass without errors when run with python3.
  </action>
  <verify>
    <automated>python3 /home/chris/gitrepos/saleae_tdm_analyer/hla/TdmWavExport.py</automated>
  </verify>
  <done>
    parse_slot_spec and _as_signed functions exist at module level. __init__ initializes all nine Phase 9 state attributes. python3 hla/TdmWavExport.py exits 0 with all assert statements passing, including the order-preservation case parse_slot_spec("4,2,0") == [4, 2, 0].
  </done>
</task>

<task type="auto">
  <name>Task 2: WAV lifecycle methods and full decode() implementation</name>
  <files>hla/TdmWavExport.py</files>
  <action>
    Add four private methods to TdmWavExport, then replace the decode() body. All code goes INSIDE the class.

    Method 1: _open_wav(self, sample_rate: int) -> None
    - n_channels = len(self._slot_list)
    - sample_width = self._bit_depth // 8  (2 for 16-bit, 4 for 32-bit)
    - self._wav = wave.open(self._output_path, 'wb')  — MUST use 'wb' not 'w'
    - self._wav.setnchannels(n_channels)
    - self._wav.setsampwidth(sample_width)
    - self._wav.setframerate(sample_rate)
    - Do NOT use a context manager (with wave.open(...) as wav:) — the file must stay open across multiple decode() calls

    Method 2: _try_derive_sample_rate(self, frame) -> None
    - slot = frame.data['slot']
    - If slot not in self._timing_ref: store self._timing_ref[slot] = frame.start_time and return
    - If self._sample_rate is not None: return (already derived)
    - delta_sec = float(frame.start_time - self._timing_ref[slot])
    - If delta_sec <= 0: return (same timestamp — skip to avoid division by zero)
    - derived = round(1.0 / delta_sec)
    - If derived < 1000 or derived > 200000: self._sample_rate = 48000 (sanity clamp — fallback for bad timing)
    - Else: self._sample_rate = derived

    Method 3: _write_wav_frame(self) -> None
    - fmt = '<' + ('h' if self._bit_depth == 16 else 'i') * len(self._slot_list)
    - samples = [self._accum.get(slot, 0) for slot in self._slot_list]  (REQ-11: silence for missing slots)
    - packed = struct.pack(fmt, *samples)
    - self._wav.writeframes(packed)  (wave module calls _patchheader() after every writeframes() — satisfies REQ-14)
    - self._frame_count += 1

    Method 4: _try_flush(self, current_frame_num: int) -> None
    - If self._last_frame_num is None: return (first frame ever — nothing to flush)
    - If current_frame_num == self._last_frame_num: return (same TDM frame — still accumulating)
    - A new TDM frame has started — flush the completed accumulator:
      - If self._sample_rate is not None and self._wav is None: call self._open_wav(self._sample_rate)
      - If self._wav is not None: call self._write_wav_frame()
      - Reset: self._accum = {}

    Replace decode() body (keep the existing `if frame.type != 'slot': return None` guard, then add):
    - slot = frame.data['slot']
    - frame_num = frame.data['frame_number']

    - Skip non-selected slots (REQ-10):
      if slot not in self._slot_set: return None

    - Accumulate sample (REQ-11: error frames contribute silence by not writing to accum):
      if not (frame.data.get('short_slot') or frame.data.get('bitclock_error')):
          self._accum[slot] = _as_signed(frame.data['data'], self._bit_depth)

    - Derive sample rate from frame timing (REQ-13):
      self._try_derive_sample_rate(frame)

    - Detect TDM frame boundary and flush completed frame (REQ-15):
      self._try_flush(frame_num)

    - Update frame tracker:
      self._last_frame_num = frame_num

    - return None

    Remove the "Phase 9 adds: ..." and "Phase 9 replaces this" comments — they are no longer accurate.

    Do NOT add any additional HLA settings. Do NOT use wave.open() as a context manager. Do NOT call writeframesraw() — always use writeframes(). Do NOT open the file in __init__.
  </action>
  <verify>
    <automated>python3 -m py_compile /home/chris/gitrepos/saleae_tdm_analyer/hla/TdmWavExport.py && python3 /home/chris/gitrepos/saleae_tdm_analyer/hla/TdmWavExport.py</automated>
  </verify>
  <done>
    py_compile reports no syntax errors. The __main__ test block still passes. TdmWavExport class contains _open_wav, _try_derive_sample_rate, _write_wav_frame, _try_flush, and a complete decode() body. No "no-op scaffold" comments remain in the file.
  </done>
</task>

</tasks>

<verification>
After both tasks complete, run the following checks:

1. Syntax and self-test pass:
   python3 -m py_compile hla/TdmWavExport.py && python3 hla/TdmWavExport.py

2. All required state attributes present in __init__:
   python3 -c "
   import ast, sys
   tree = ast.parse(open('hla/TdmWavExport.py').read())
   cls = next(n for n in ast.walk(tree) if isinstance(n, ast.ClassDef) and n.name == 'TdmWavExport')
   init = next(n for n in cls.body if isinstance(n, ast.FunctionDef) and n.name == '__init__')
   assigns = [n.targets[0].attr for n in ast.walk(init) if isinstance(n, ast.Assign) and hasattr(n.targets[0], 'attr')]
   required = {'_slot_list', '_slot_set', '_wav', '_sample_rate', '_accum', '_last_frame_num', '_timing_ref', '_frame_count'}
   missing = required - set(assigns)
   if missing: sys.exit(f'Missing __init__ attrs: {missing}')
   print('All required __init__ attrs present')
   "

3. Required methods present:
   python3 -c "
   import ast, sys
   tree = ast.parse(open('hla/TdmWavExport.py').read())
   cls = next(n for n in ast.walk(tree) if isinstance(n, ast.ClassDef) and n.name == 'TdmWavExport')
   methods = {n.name for n in cls.body if isinstance(n, ast.FunctionDef)}
   required = {'__init__', 'decode', '_open_wav', '_try_derive_sample_rate', '_write_wav_frame', '_try_flush'}
   missing = required - methods
   if missing: sys.exit(f'Missing methods: {missing}')
   print('All required methods present:', methods)
   "

4. Module-level functions present:
   python3 -c "
   import ast, sys
   tree = ast.parse(open('hla/TdmWavExport.py').read())
   funcs = {n.name for n in tree.body if isinstance(n, ast.FunctionDef)}
   required = {'parse_slot_spec', '_as_signed'}
   missing = required - funcs
   if missing: sys.exit(f'Missing module-level functions: {missing}')
   print('Module-level helpers present:', funcs)
   "

5. writeframes (not writeframesraw) used for WAV writes:
   grep -n 'writeframes' hla/TdmWavExport.py
   (Must see writeframes, must NOT see writeframesraw)
</verification>

<success_criteria>
- python3 -m py_compile hla/TdmWavExport.py exits 0 (no syntax errors)
- python3 hla/TdmWavExport.py exits 0 (all assert-based self-tests pass)
- parse_slot_spec and _as_signed exist at module level
- TdmWavExport.__init__ initializes all eight state attributes
- TdmWavExport contains _open_wav, _try_derive_sample_rate, _write_wav_frame, _try_flush, and a complete decode()
- decode() returns None for all frames (no HLA output frames generated — WAV is the output)
- wave.open() is called with 'wb' mode, not used as a context manager
- writeframes() used for all WAV writes (not writeframesraw)
- REQ-08 through REQ-15 all satisfied by the implementation
</success_criteria>

<output>
After completion, create `.planning/phases/09-core-wav-writing/09-01-SUMMARY.md` following the summary template at @/home/chris/.claude/get-shit-done/templates/summary.md
</output>
