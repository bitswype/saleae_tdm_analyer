---
phase: 05-framev2-enrichment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/TdmAnalyzer.cpp
  - CHANGELOG.md
autonomous: true
requirements:
  - FRM2-01
  - FRM2-02
  - FRM2-03
  - FRM2-04
  - FRM2-05
  - FRM2-06
  - FRM2-07

must_haves:
  truths:
    - "Every decoded slot row in the Logic 2 data table shows boolean columns short_slot, extra_slot, bitclock_error, missed_data, and missed_frame_sync — including error-free rows (which show false)"
    - "The data table shows a slot column with 0-based values — no channel column exists"
    - "The data table shows a severity column with values error, warning, or ok — no errors or warnings string columns exist"
    - "An HLA script can access frame.data['short_slot'] without KeyError on any decoded slot frame"
    - "All nine FrameV2 fields are emitted on every slot frame regardless of error state"
  artifacts:
    - path: "src/TdmAnalyzer.cpp"
      provides: "Rewritten FrameV2 block in AnalyzeTdmSlot() with nine-field schema"
      contains: "AddBoolean.*short_slot"
    - path: "CHANGELOG.md"
      provides: "Breaking change documentation for FrameV2 schema replacement"
      contains: "severity"
  key_links:
    - from: "src/TdmAnalyzer.cpp"
      to: "mResultsFrame.mFlags"
      via: "bitmask reads for boolean field values"
      pattern: "mResultsFrame\\.mFlags & (SHORT_SLOT|UNEXPECTED_BITS|BITCLOCK_ERROR|MISSED_DATA|MISSED_FRAME_SYNC)"
    - from: "src/TdmAnalyzer.cpp"
      to: "FrameV2::AddBoolean"
      via: "SDK FrameV2 boolean field emission"
      pattern: "frame_v2\\.AddBoolean"
    - from: "src/TdmAnalyzer.cpp"
      to: "FrameV2::AddString severity"
      via: "severity enum string derived from flag bitmask"
      pattern: 'frame_v2\.AddString.*"severity"'
---

<objective>
Replace the FrameV2 output block in AnalyzeTdmSlot() with a nine-field schema: slot (0-based integer replacing channel), data (unchanged), frame_number (unchanged), severity (error/warning/ok string replacing errors+warnings strings), and five boolean error fields (short_slot, extra_slot, bitclock_error, missed_data, missed_frame_sync). All fields emitted on every slot frame. Document the breaking changes in CHANGELOG.md.

Purpose: Enable HLA scripts to filter error frames programmatically using boolean fields and a severity enum, without string parsing.
Output: Modified src/TdmAnalyzer.cpp with nine-field FrameV2 schema; updated CHANGELOG.md with breaking change documentation.
</objective>

<execution_context>
@/home/chris/.claude/get-shit-done/workflows/execute-plan.md
@/home/chris/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-framev2-enrichment/05-CONTEXT.md
@.planning/phases/05-framev2-enrichment/05-RESEARCH.md
@src/TdmAnalyzer.cpp
@src/TdmAnalyzerResults.h
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite FrameV2 block in AnalyzeTdmSlot() with nine-field schema</name>
  <files>src/TdmAnalyzer.cpp</files>
  <action>
In `src/TdmAnalyzer.cpp`, function `AnalyzeTdmSlot()`, replace the entire FrameV2 block (approximately lines 302–340, from `FrameV2 frame_v2;` through the `mResults->AddFrameV2(...)` call) with the nine-field schema. The replacement must:

1. **Remove** the `char error_str[80]` and `char warning_str[32]` declarations.
2. **Remove** the entire `snprintf` loop that builds `error_str` and `warning_str` (the `size_t used = 0;` block through the `UNEXPECTED_BITS` snprintf).
3. **Remove** the two `AddString("errors", ...)` and `AddString("warnings", ...)` calls.
4. **Replace** `frame_v2.AddInteger("channel", mResultsFrame.mType)` with `frame_v2.AddInteger("slot", mResultsFrame.mType)`.
5. **Keep** the `adjusted_value` computation and `frame_v2.AddInteger("data", adjusted_value)` exactly as-is.
6. **Move** `frame_v2.AddInteger("frame_number", mFrameNum)` to appear after "data" and before "severity" (it was previously at the end).
7. **Add** severity string derivation using `const char*` literals and an if/else chain:
   - Extract five booleans from `mResultsFrame.mFlags` using the existing flag macros:
     - `bool is_short_slot = (mResultsFrame.mFlags & SHORT_SLOT) != 0;`
     - `bool is_extra_slot = (mResultsFrame.mFlags & UNEXPECTED_BITS) != 0;`
     - `bool is_bitclock_error = (mResultsFrame.mFlags & BITCLOCK_ERROR) != 0;`
     - `bool is_missed_data = (mResultsFrame.mFlags & MISSED_DATA) != 0;`
     - `bool is_missed_frame_sync = (mResultsFrame.mFlags & MISSED_FRAME_SYNC) != 0;`
   - Severity logic (error wins): if any of `is_short_slot || is_bitclock_error || is_missed_data || is_missed_frame_sync` → `"error"`; else if `is_extra_slot` → `"warning"`; else → `"ok"`.
   - `frame_v2.AddString("severity", severity);`
8. **Add** five `AddBoolean()` calls in order: `short_slot`, `extra_slot`, `bitclock_error`, `missed_data`, `missed_frame_sync`.
9. **Keep** the `mResults->AddFrameV2(frame_v2, "slot", ...)` call unchanged (the type string "slot" is already correct).

The final field emission order must be: slot → data → frame_number → severity → short_slot → extra_slot → bitclock_error → missed_data → missed_frame_sync.

**CRITICAL anti-patterns to avoid:**
- Do NOT conditionally emit any field — all nine fields on every frame.
- Do NOT modify anything in `TdmAnalyzerResults.cpp` — that is FrameV1 code.
- Do NOT modify the `TdmAnalyzer()` constructor or remove the `UseFrameV2()` call.
- Do NOT change `mResults->AddFrame(mResultsFrame)` or the FrameV1 mResultsFrame setup above the FrameV2 block.
- Do NOT change `mResultsFrame.mFlags` computation or `DISPLAY_AS_ERROR_FLAG`/`DISPLAY_AS_WARNING_FLAG` flag logic — those are FrameV1 display hints.

Match the existing code style: 4-space indentation, spaces inside parentheses for SDK calls (e.g., `frame_v2.AddInteger( "slot", mResultsFrame.mType )`), no spaces inside parentheses for boolean expressions.
  </action>
  <verify>
    <automated>cd /home/chris/gitrepos/saleae_tdm_analyer && cmake -B build -DCMAKE_BUILD_TYPE=Release 2>&1 | tail -5 && cmake --build build 2>&1 | tail -10</automated>
    <manual>Verify the FrameV2 block contains exactly 9 AddField calls (1 AddString for severity, 3 AddInteger for slot/data/frame_number, 5 AddBoolean) and no AddString for errors/warnings</manual>
  </verify>
  <done>
    - src/TdmAnalyzer.cpp compiles without errors or warnings
    - The FrameV2 block contains exactly nine field emission calls in the decided order
    - No `channel`, `errors`, or `warnings` keys remain in the FrameV2 block
    - No `error_str` or `warning_str` char buffers remain in AnalyzeTdmSlot()
    - Severity logic checks error flags before warning flag (error wins)
    - All boolean fields use AddBoolean (not AddInteger)
    - TdmAnalyzerResults.cpp is unmodified
  </done>
</task>

<task type="auto">
  <name>Task 2: Document breaking FrameV2 schema changes in CHANGELOG.md</name>
  <files>CHANGELOG.md</files>
  <action>
Update `CHANGELOG.md` to document the FrameV2 schema changes under the `[Unreleased]` section (add one if it does not exist; if it does, append to it). If there is no `[Unreleased]` section, add it between the header and the existing `[2.0.0]` entry.

Add the following under `[Unreleased]`:

### Changed
- **FrameV2 schema:** Replaced `channel` (integer) field with `slot` (integer, same 0-based value) [FRM2-06]
- **FrameV2 schema:** Replaced `errors` and `warnings` string fields with `severity` string field (`error`/`warning`/`ok`) [FRM2-07]

### Added
- **FrameV2 schema:** Added boolean fields `short_slot`, `extra_slot`, `bitclock_error`, `missed_data`, `missed_frame_sync` to every decoded slot row [FRM2-01 through FRM2-05]

### Migration
Include a brief HLA migration note similar to the v2.0.0 pattern:

```python
# Before (v2.0.0)
channel = frame.data["channel"]
errors = frame.data["errors"]    # string like "E: Short Slot E: Data Error "
warnings = frame.data["warnings"]  # string like "W: Extra Slot "

# After (unreleased)
slot = frame.data["slot"]           # same 0-based integer value
severity = frame.data["severity"]   # "error", "warning", or "ok"
is_short = frame.data["short_slot"] # True or False
is_extra = frame.data["extra_slot"] # True or False
```

Per user decision: no version bump per-phase (accumulate to milestone end). No README FrameV2 schema update.
  </action>
  <verify>
    <automated>cd /home/chris/gitrepos/saleae_tdm_analyer && grep -c "severity" CHANGELOG.md && grep -c "short_slot" CHANGELOG.md && grep -c "\[Unreleased\]" CHANGELOG.md</automated>
    <manual>Verify CHANGELOG.md has an Unreleased section documenting the field replacements and additions</manual>
  </verify>
  <done>
    - CHANGELOG.md contains an [Unreleased] section with the FrameV2 schema changes
    - All seven requirement IDs (FRM2-01 through FRM2-07) are referenced in the changelog entries
    - Migration example shows before/after HLA Python code
    - No version bump applied (per user decision — accumulate to milestone end)
  </done>
</task>

</tasks>

<verification>
1. `cmake --build build` succeeds with no errors or warnings
2. `grep -c "AddBoolean" src/TdmAnalyzer.cpp` returns 5 (one per boolean field)
3. `grep -c "AddString.*severity" src/TdmAnalyzer.cpp` returns 1
4. `grep -c "AddInteger.*slot" src/TdmAnalyzer.cpp` returns 1 (renamed from channel)
5. `grep "errors\|warnings\|channel" src/TdmAnalyzer.cpp` shows no FrameV2 references to old field names (only FrameV1 in mResultsFrame context, if any)
6. `grep -c "\[Unreleased\]" CHANGELOG.md` returns 1
7. No changes to `src/TdmAnalyzerResults.cpp` — verify with `git diff --name-only` after commits
</verification>

<success_criteria>
- The FrameV2 block in AnalyzeTdmSlot() emits exactly nine fields in the decided order: slot, data, frame_number, severity, short_slot, extra_slot, bitclock_error, missed_data, missed_frame_sync
- All boolean fields use AddBoolean() and are emitted on every slot frame (no conditional emission)
- Severity is derived with error-wins logic from existing flag bitmask
- Old fields (channel, errors, warnings) are completely removed from the FrameV2 block
- FrameV1 code (mResultsFrame, GenerateBubbleText, GenerateFrameTabularText) is untouched
- CHANGELOG.md documents all breaking changes with migration examples
- Project compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/05-framev2-enrichment/05-01-SUMMARY.md`
</output>
